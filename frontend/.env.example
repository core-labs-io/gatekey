# Base URL of the Gatekey backend API (no trailing slash).
# In docker-compose this is the backend service's container-network address
# as seen from the browser - see ../docker-compose.yml for how this is wired
# through for local `docker-compose up` usage.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
